# Lockdown Configuration
lockdown.privateKeyPath=../lockdown_rsa

# PostgreSQL Repository Configuration
repository.pg.url=jdbc:postgresql://127.0.0.1/lrex-server
repository.pg.maxPoolSize=64
repository.pg.credentialsPath=src/test/resources/credentials.properties